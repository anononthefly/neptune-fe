machine:
  node:
    version: 0.10.25
  services:
    - docker

dependencies:
  pre:
    - node --version
  override:
    - npm install
    - docker info
    - docker build -t anononthefly/neptune-fe .

deployment:
  hub:
    branch: master
    commands:
      - docker login -e anononthefly@gmail.com -u anononthefly -p anonthefly && docker push anononthefly/neptune-fe:latest
